@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    @apply font-mono font-normal bg-sage;
    font-size: 18px;
    line-height: 24px;
  }

  h1 {
    @apply text-4xl;
  }

  h3 {
    @apply text-2xl;
  }
}
